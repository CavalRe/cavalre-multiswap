import {
    Container,
    Title
} from '@mantine/core';

const Swap = () => {

    return (
        <Container>
            <Title>Multi-Asset Swap</Title>
        </Container>
    );
};

export default Swap;
